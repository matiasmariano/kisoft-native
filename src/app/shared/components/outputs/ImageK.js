import React from 'react';
import { Image } from 'react-native-elements';

const ImageK = ({ containerStyle, source, style }) => (
    <Image
        containerStyle={containerStyle}
        source={source}
        style={style} />
)

export default ImageK